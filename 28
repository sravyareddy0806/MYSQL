SET @rowindex := -1;

SELECT ROUND(AVG(lat.LAT_N), 4)
FROM (
    SELECT @rowindex := @rowindex + 1 AS row_index, LAT_N
    FROM STATION
    ORDER BY LAT_N
) AS lat
WHERE lat.row_index IN (
    FLOOR(@rowindex / 2), 
    CEIL(@rowindex / 2)
);
